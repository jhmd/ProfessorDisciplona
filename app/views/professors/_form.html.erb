<%= form_for(@professor) do |f| %>
  <% if @professor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@professor.errors.count, "error") %> prohibited this professor from being saved:</h2>

      <ul>
      <% @professor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :login %><br>
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :cpf %><br>
    <%= f.text_field :cpf %>
  </div>
  
   
  <% Disciplina.all.each do |disciplina| -%>
    <%= check_box_tag :disciplina_ids, disciplina.id, @professor.disciplinas.include?(disciplina), :name => 'professor[disciplina_ids][]' -%>
    <%= label_tag :disciplina_ids, disciplina.nome -%>
  <% end -%>  
  
  
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
